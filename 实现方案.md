# 御连 YuLink —— 校园网自动认证常驻程序 实现方案

> 文档版本：v2.0（2026-09-08）
> 项目目录：`D:\project\win\YuLinkc`
> 说明：v2.0 将自动化核心从“Node + Playwright 独立子进程”改为“Tauri 自带隐藏 WebView2 窗口驱动”，不再打包 Node 与 Chromium。

---

## 1. 背景与目标

现有校园网登录依赖油猴脚本：用户打开浏览器访问认证页，脚本把账号、密码、电信短信动态密码填入表单并提交。目标改造为 Windows 常驻后台程序：

- 开机自动运行，无需打开浏览器、无需手动访问认证页；
- 后台通过真实浏览器环境完成认证；
- 常驻系统托盘，点击托盘打开与油猴脚本一致的设置/状态弹窗；
- 断网、认证过期后自动重新登录。

软件暂定名：**御连 YuLink**。

---

## 2. 现有油猴脚本行为分析

`登录脚本.js` 的核心事实：

1. **账号、密码、动态密码均由用户提供**；`#dynPass` 是电信短信下发的 6 位动态密码输入框，不是图片验证码；
2. **软件只做填表与提交**：元素查找 → 填入已保存值 → 点击登录；
3. **验证码有效期管理**：默认 27 小时，预设 1/3/9/18/27 小时、永久、自定义；有效期内复用；
4. **结果判定**：轮询 `#message`，10 秒无错误文案视为成功；
5. 运营商固定中国电信（`ISP_select = "2"`）。

因此：**不需要 OCR、不需要验证码图片、不需要逆向接口**。真正操作页面的方法极少（`querySelector`、`value` 赋值、事件派发、`click`、`innerText`、轮询），这使“在 WebView2 中注入同样逻辑”成为低成本可行路线。

---

## 3. 需求确认（定稿）

| # | 需求 | 说明 |
|---|---|---|
| 1 | 托盘常驻 | 默认只显示托盘图标；点击托盘打开设置弹窗 |
| 2 | 弹窗 UI | 界面设计与油猴脚本弹窗一致：账号、密码、动态密码、有效期、状态、立即登录 |
| 3 | 按介质选认证页 | 无线 → `http://172.26.255.2`；有线 → `http://172.26.255.3` |
| 4 | 后台自动登录 | 由 Tauri 的隐藏 WebView2 窗口（系统内置 Chromium）加载认证页并自动执行填表提交 |
| 5 | 动态密码由用户提供 | 软件只填入与做有效期管理，不识别、不猜测 |
| 6 | 开机自启 | 用户登录后自动启动并完成认证 |
| 7 | 断网自动恢复 | 外网中断且认证页可达时自动重登 |
| 8 | 不做逆向 | 不抓包、不分析接口、不模拟 POST |
| 9 | 配置与日志 | DPAPI 加密配置；轮转日志 |

---

## 4. 总体架构（v2.0）

```text
┌──────────────────────────── 御连（单个 Tauri 2 进程）────────────────────────────┐
│                                                                                    │
│  Rust 主进程                                                                       │
│  ├─ 托盘 / 自启 / 单实例                                                           │
│  ├─ 配置加密存取（DPAPI）/ 日志                                                    │
│  ├─ 网络介质识别（有线/无线） + 双探活                                             │
│  ├─ 登录状态机与定时调度                                                           │
│  ├─ 本地回传端口（127.0.0.1，接收页面结果）                                        │
│  └─ 认证 WebView 管理（创建/注入/超时/销毁/重试）                                   │
│                                                                                    │
│  WebView 窗口 A：主界面（Vue 3，设置/状态弹窗）                                    │
│  WebView 窗口 B：隐藏认证窗口 —— 直接加载校园网认证页                               │
│      └─ 注入 JS：填账号/密码/ISP/动态密码 → 提交 → 结果回传                         │
└────────────────────────────────────────────────────────────────────────────────────┘
```

关键点：

- 窗口 B 与窗口 A 共用**同一个系统 WebView2 Runtime**（Windows 内置 Chromium），不额外下载、不打包任何浏览器；
- 认证窗口平时不存在，只在“需要登录”时创建，登录结束立即销毁；
- 门户页面**拿不到 Tauri 权限**，只能执行注入的填表脚本并通过本地回传端口报告结果，凭据不外泄；
- 页面内不做最终网络判定；登录后由 Rust 直接探测外网确认。

---

## 5. 可行性分析

### 5.1 结论

**技术上可行**。依据：

1. WebView2 是完整 Chromium 渲染引擎，油猴脚本使用的 DOM 方法全部原生支持；
2. Tauri 2 支持创建加载远程 URL 的窗口（`WebviewUrl::External`），并可从 Rust 对该窗口 `eval()` 注入 JS；
3. 门户为明文 HTTP（`http://172.26.255.x`），页面脚本通过 `new Image().src` 请求本机 `127.0.0.1` 端口即可回传结果，既不受跨域限制也不触发混合内容拦截；
4. Tauri 2 的能力（Capability）体系支持为远程 URL 授权，若未来需要页面直接调用 Tauri API（事件/存储）也有官方通道；
5. 注入脚本可由 `登录脚本.js` 的自动化部分裁剪而来，预计 100~150 行。

### 5.2 关键风险与对策

| 风险 | 说明 | 对策 |
|---|---|---|
| 隐藏 WebView2 不执行 eval | 已知现象：控制器隐藏时 `ExecuteScript/eval` 可能被 no-op | 认证窗口不使用 `visible(false)`，改为**屏幕外可见窗口**（如坐标 -32000, -32000、无边框、不进任务栏），或用“先显示后自动化”的开关；POC 第 1 项验证 |
| eval 无返回值 | Tauri `eval()` 单向执行，拿不到表达式结果 | 注入脚本自跑完整流程，结果经本地回传端口上报；Rust 侧另有外网探测作最终确认 |
| 页面加载时机 | 注入太早脚本会失败 | 注入脚本自带幂等守卫 + 轮询 `#f1_div`（沿用油猴逻辑）；Rust 每 300~500ms 重试注入，直到收到“已开始”回传 |
| 页面挂起/崩溃 | 门户无响应或 WebView2 进程异常 | Rust 侧超时（默认 60s）后销毁窗口；进程崩溃由 Tauri 事件感知；退避重试并记录日志 |
| 远程页面能力误用 | 门户脚本可能被注入代码触碰 | 不给门户授权任何 Tauri 能力；只注入最小填表逻辑；凭据仅存在于页面输入框与内存 |
| 门户改为 HTTPS | HTTP 页面加载本机 HTTP 信标无限制；HTTPS 页面会拦混合内容 | 回传层做成可切换（Image 信标 / WebSocket / 远程 IPC），POC 同时验证；现状为 HTTP，无影响 |
| 会话 Cookie 残留 | 登录状态可能跨重启残留 | 持久化 profile 由 WebView2 管理；每次启动先探活，已在线则不执行登录 |

---

## 6. 技术选型（v2.0 变更）

| 层 | 技术 | 说明 |
|---|---|---|
| 应用壳 | Tauri 2（Rust） | 唯一进程 |
| 界面 | Vue 3 + TypeScript | 复刻油猴弹窗 UI |
| 浏览器引擎 | 系统 WebView2 Runtime | Windows 内置 Chromium，Tauri 本就依赖，零额外体积 |
| 页面驱动 | Rust `eval()` 注入 JS | 等价于“控制台执行油猴逻辑” |
| 结果回传 | 本地 127.0.0.1 回传端口 | 首选 Image 信标；备用 WebSocket/远程 IPC |
| 网络探测 | Rust 侧 HTTP 探测 | 页面不可信，最终判定归 Rust |
| 配置加密 | Windows DPAPI | 密码与动态密码加密落盘 |
| 打包 | Tauri NSIS 安装包 | 只需 exe + 前端资源，预计几 MB ~ 十几 MB |

**已移除**：Node.js、playwright-core、内置 Chromium、sidecar 进程协议。

---

## 7. 目录结构

```text
YuLinkc/
├── 登录脚本.js              # 保留，逻辑与 UI 迁移参考
├── 实现方案.md              # 本文档
│
├── src/                     # Vue 主界面（设置/状态弹窗）
│   ├── App.vue
│   └── ...
│
├── src-tauri/
│   ├── Cargo.toml
│   ├── tauri.conf.json      # 主窗口、托盘图标、资源
│   ├── capabilities/default.json
│   └── src/
│       ├── main.rs
│       ├── lib.rs           # Builder：托盘、插件、命令、启动调度
│       ├── auth_window.rs   # ★ 认证窗口生命周期（创建/注入/销毁/重试）
│       ├── inject.rs        # 注入脚本模板（读取 assets/auth.js 并填入凭据）
│       ├── bridge.rs        # ★ 本地回传端口（接收页面结果事件）
│       ├── network.rs       # 介质识别（有线/无线）+ 双探活
│       ├── state.rs         # 状态机与事件分发
│       ├── store.rs         # DPAPI 加密配置
│       └── scheduler.rs     # 定时巡检 / 断线触发
│
├── assets/auth/             # 注入用前端资源
│   └── auth.js              # ★ 页面自动化脚本（油猴逻辑裁剪版）
│
├── mock/                    # 本地模拟认证页（开发/自动化测试）
│   ├── wireless.html        # 复刻 172.26.255.2 页面结构
│   └── wired.html           # 复刻 172.26.255.3 页面结构
│
└── docs/
    └── 联调记录.md          # 现场 DOM 结构记录
```

---

## 8. 认证窗口与注入设计

### 8.1 认证窗口

- 类型：`WebviewWindowBuilder` + `WebviewUrl::External`；
- 形态：屏幕外（-32000, -32000）、无边框、不可缩放、不进任务栏/Alt-Tab；**保持可见状态**以规避隐藏不执行 `eval` 的问题；
- 生命周期：创建 → 注入 → 收结果 → 超时判定 → 关闭销毁；
- profile：使用默认持久化目录，保存 Cookie；窗口标签固定（如 `auth`），避免重复创建。

### 8.2 注入脚本（`assets/auth/auth.js`）

由 `登录脚本.js` 裁剪：

```js
// 1) 幂等守卫：只执行一次
if (window.__YL_AUTH_RUNNING__) return;
window.__YL_AUTH_RUNNING__ = true;

// 2) 凭据由 Rust 注入，页面不落盘
const CFG = window.__YL_CFG__; // { account, password, isp, smsCode, beacon, timeoutMs }

// 3) 结果回传（Image 信标，GET 请求，无 CORS 限制）
function report(state, msg) {
  try { new Image().src = CFG.beacon + "?state=" + state + "&msg=" + encodeURIComponent(msg || ""); } catch (e) {}
}

// 4) 以下逻辑与油猴脚本一致：
//    waitFor("#f1_div") → 填账号/密码/ISP → 填 #dynPass → 点 #login_btn
//    → 轮询 #message：有文案 report("failed", text)；
//      否则等待并 report("ok")
```

选择器全部读取注入配置，学校门户改版只改配置，不改程序。

### 8.3 双向桥（bridge.rs）

- 登录前 Rust 在 `127.0.0.1:0`（随机端口）启动一次性 TCP 监听；
- 页面通过 `http://127.0.0.1:<port>/report?state=...&msg=...` 上报；
- Rust 解析后转成状态机事件，随后关闭监听；
- 信标只需解析一行 HTTP GET，不引入第三方依赖；
- 备用通道：WebSocket / Tauri 远程 IPC（POC 阶段验证其一即可，首选 Image 信标）。

---

## 9. 核心流程

### 9.1 首次使用

```text
点击托盘 → 打开设置弹窗
  → 填写账号、密码
  → 点击“测试登录”
  → 无有效动态密码 → 提示输入电信下发的 6 位数字
  → 保存（记录时间，开始计有效期）
  → 创建隐藏认证窗口 → 自动登录 → 状态“已连接”
```

### 9.2 开机自动登录

```text
Windows 用户登录
  → 御连自启，仅托盘
  → 等待网络就绪（退避重试）
  → 识别介质：无线 → .2，有线 → .3
  → 双探活：外网通 → 待命巡检；
            外网不通 + 认证页可达 → 进入登录
  → 创建隐藏认证窗口加载认证页 → 注入脚本
  → 动态密码有效 → 全自动完成登录
  → 页面回传 + Rust 外网探测双确认 → 销毁窗口 → “已连接”
```

动态密码过期：托盘提醒，不阻塞；用户补码后自动继续。

### 9.3 断网自动恢复

```text
巡检（在线 120s / 离线 30s，可配置）
  → 外网断 + 认证页可达 → 自动重登（复用有效动态密码）
  → 失败/过期 → 提醒补码
  → 恢复 → “已连接”
```

网络插拔、Wi-Fi 切换、休眠唤醒等事件立即触发一次检查。

### 9.4 介质识别规则

| 活动网卡类型 | 判定 | 认证页 |
|---|---|---|
| Wi-Fi（IEEE80211） | 无线 | `http://172.26.255.2` |
| 以太网 | 有线 | `http://172.26.255.3` |
| VMware/Tailscale/TAP 等虚拟网卡 | 忽略 | — |

双网卡并存时默认按“活动默认路由”判定，可配置“优先有线”或手动指定。

---

## 10. 认证页元素映射（自 `登录脚本.js` 迁移）

| 脚本选择器 | 含义 | 注入脚本用法 |
|---|---|---|
| `#f1_div form input:nth-of-type(3)` | 账号框 | 赋值 + 派发 input 事件 |
| `#f1_div form input:nth-of-type(4)` | 密码框 | 同上 |
| `#f1_div select[name='ISP_select']` | 运营商下拉 | 选 `2`（电信，可配置） |
| `#dynPass` | 动态密码框 | 填入用户提供的 6 位数字 |
| `#login_btn` | 登录按钮 | click + mousedown/mouseup/click 完整事件 |
| `#message` | 失败消息 | 非空 → 失败；超时无 → 配合外网探测确认 |
| `http://172.26.255.2` / `.3` | 无线/有线认证页 | 按介质选择 |

---

## 11. 配置与存储

目录：`%APPDATA%\YuLink\`

```json
{
  "account": {
    "username": "2024xxxx",
    "passwordEnc": "base64:dpapi:...",
    "isp": "2"
  },
  "smsCode": {
    "codeEnc": "base64:dpapi:...",
    "savedAt": 1780000000000,
    "hours": 27
  },
  "preferences": {
    "autostart": true,
    "adapterMode": "auto",
    "portalWireless": "http://172.26.255.2/",
    "portalWired": "http://172.26.255.3/",
    "checkIntervalOnlineSec": 120,
    "checkIntervalOfflineSec": 30,
    "loginMaxRetries": 3
  }
}
```

约束：密码/动态密码 DPAPI 加密；日志无明文；门户报“验证码错误/失效”立即作废动态密码并请求补码。

---

## 12. 托盘与界面

- 托盘图标随状态变色：绿=已连接、黄=需动态密码、红=错误；
- 左键单击：打开设置弹窗（已有则置前）；右键菜单：立即登录 / 打开设置 / 开机自启 / 退出；
- 弹窗视觉复刻油猴脚本：账号、密码（可切换明文）、动态密码 + 剩余有效期、有效期下拉、开机自启开关、状态、立即登录；
- 状态取值：未配置 / 网络未就绪 / 登录中 / 需动态密码 / 已连接 / 登录失败（含原因）。

---

## 13. 打包与部署

- NSIS 每用户安装包，产物仅 exe + 前端资源 + 注入脚本，预计几 MB ~ 十几 MB；
- 前置依赖：WebView2 Runtime（Tauri 2 必需，Win10/11 绝大多数已内置；安装器可含 Evergreen Bootstrapper 兜底）；
- 自启：HKCU Run（autostart 插件），卸载清理；单实例防重复登录。

---

## 14. 开发与测试计划

### 14.1 POC（先行验证，约半天）

1. 验证“屏幕外可见窗口”下 `eval()` 注入可执行（规避隐藏 no-op）；
2. 用 mock 认证页验证注入脚本：填表 → 提交 → Image 信标回传成功；
3. 验证窗口创建/销毁循环 10 次无泄漏、无残留进程；
4. 若 1 不通过，退回方案：登录瞬间置顶显示 1×1 窗口后再隐藏，或改 Playwright + 系统 Edge。

### 14.2 Mock 测试

`mock/wireless.html`、`mock/wired.html` 复刻真实页面 DOM（两种结果分支），离线跑通全链路。

### 14.3 校园网现场联调

实测记录（2026-09-08）：本机在 172.29.x 网段，`172.26.255.2/.3` 不可达，须现场执行：

1. 保存真实登录页 HTML（.2 与 .3）至 `docs/`；
2. 校准选择器与超时参数；
3. 分别验证无线/有线开机登录、动态密码过期补码流程；
4. 验证断网自愈与重启残留 Cookie 行为。

---

## 15. 里程碑

| 阶段 | 内容 | 完成标准 |
|---|---|---|
| P0 | POC | 隐藏窗口注入 + mock 登录 + 回传链路全部跑通 |
| M1 | Rust 骨架 | 托盘、自启、单实例、配置加密、日志 |
| M2 | UI | Vue 弹窗复刻油猴脚本，状态联动 |
| M3 | 集成 | 介质识别 + 双探活 + 认证窗口状态机 + 自动重登 |
| M4 | 现场联调 | 真实校园网无线/有线验收 |
| M5 | 打包发布 | NSIS 安装包、开机自启验收 |

---

## 16. 风险与待确认项

1. **隐藏窗口 eval 行为**：POC 必须首先验证，若有问题采用屏幕外/临时显示对策；
2. **真实页面 DOM**：选择器以油猴脚本为准，现场需保存 HTML 校准；
3. **动态密码实际有效期**：默认 27 小时，UI 可调；
4. **双网卡**：默认按活动默认路由，可配置；
5. **门户行为变化**：若门户禁止复用动态密码，程序退化为“提醒式自动填表”，不假报成功。

